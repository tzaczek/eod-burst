{
  "Kafka": {
    "BootstrapServers": "kafka:29092"
  },
  "SqlServer": {
    "ConnectionString": "Server=sqlserver,1433;Database=EodTrades;User Id=sa;Password=${MSSQL_SA_PASSWORD};TrustServerCertificate=true;"
  },
  "Minio": {
    "Endpoint": "minio:9000",
    "AccessKey": "${MINIO_ROOT_USER}",
    "SecretKey": "${MINIO_ROOT_PASSWORD}"
  }
}
